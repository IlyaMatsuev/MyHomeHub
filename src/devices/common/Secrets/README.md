# Secrets

This library is only used to provide an access to some sensitive data to devices developed in this project.

## 🛠️ Requisites

All credentials and private keys used to connect to the hub are stored in `src/secrets.cpp` (which is not in the repo, ofc).

You can check the required secrets in the `secrets.h` header.
